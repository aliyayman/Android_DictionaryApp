<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aliyayman.sozluk4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sozluk4">
        <activity android:name=".Activitys.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.QuizeActivity" />
        <activity android:name=".Activitys.ResultActivity" />
        <activity android:name=".Activitys.QuizeAnaActivity" />
        <activity android:name=".Activitys.KelimeActivity" />
        <activity android:name=".Activitys.MainActivity">

        </activity>
    </application>

</manifest>